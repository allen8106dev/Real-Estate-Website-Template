import { Mail,Phone,MessageCircle } from 'lucide-react'; import type { Agent } from '@/types/agent';
export function AgentCard({agent}:{agent:Agent}){return <article className="card"><img className="aspect-[.9] w-full object-cover" src={agent.image} alt={agent.name} loading="lazy"/><div className="p-5"><h3 className="font-display text-xl">{agent.name}</h3><p className="mt-1 text-sm text-gold">{agent.role}</p><p className="mt-3 text-sm leading-6 text-slate">{agent.areas.join(' · ')}</p><div className="mt-4 flex gap-3"><a aria-label={`Call ${agent.name}`} href={`tel:${agent.phone}`}><Phone size={18}/></a><a aria-label={`Email ${agent.name}`} href={`mailto:${agent.email}`}><Mail size={18}/></a><a aria-label={`WhatsApp ${agent.name}`} href={`https://wa.me/${agent.whatsapp}`}><MessageCircle size={18}/></a></div></div></article>}
