import { Link } from 'react-router-dom'; import { BedDouble,Bath,Maximize,MapPin } from 'lucide-react'; import type { Property } from '@/types/property'; import { formatArea,formatCurrency } from '@/lib/format';
export function PropertyCard({property}:{property:Property}){return <article className="card group"><Link to={`/properties/${property.slug}`} className="block aspect-[1.35] overflow-hidden"><img className="h-full w-full object-cover transition duration-500 group-hover:scale-105" src={property.images[0]} alt={property.title} loading="lazy"/></Link><div className="p-5"><div className="mb-3 flex gap-2"><span className="rounded-pill bg-ink px-2.5 py-1 text-[10px] font-bold uppercase tracking-wider text-white">For {property.priceType}</span>{property.featured&&<span className="rounded-pill bg-gold/15 px-2.5 py-1 text-[10px] font-bold uppercase tracking-wider text-gold">Featured</span>}</div><Link to={`/properties/${property.slug}`}><h3 className="font-display text-xl leading-tight hover:text-gold">{property.title}</h3></Link><p className="mt-2 flex items-center gap-1 text-sm text-slate"><MapPin size={14}/>{property.location}, {property.city}</p><p className="mt-4 font-display text-xl">{formatCurrency(property.price,property.priceType)}</p><div className="mt-4 flex border-t border-ink/10 pt-4 text-xs text-slate">{property.bedrooms&&<span className="mr-4 flex items-center gap-1"><BedDouble size={15}/>{property.bedrooms} Bed</span>}{property.bathrooms&&<span className="mr-4 flex items-center gap-1"><Bath size={15}/>{property.bathrooms} Bath</span>}<span className="flex items-center gap-1"><Maximize size={14}/>{formatArea(property.areaSqFt)}</span></div></div></article>}
