import { useSearchParams } from 'react-router-dom'; import { properties } from '@/data/properties';
export function usePropertyFilters(){const [params,setParams]=useSearchParams(); const filters={location:params.get('location')||'',type:params.get('type')||'',purpose:params.get('purpose')||'',bedrooms:params.get('bedrooms')||'',sort:params.get('sort')||'newest'}; const results=properties.filter(p=>(!filters.location||p.location===filters.location)&&(!filters.type||p.type===filters.type)&&(!filters.purpose||p.priceType===filters.purpose)&&(!filters.bedrooms||p.bedrooms===Number(filters.bedrooms))).sort((a,b)=>filters.sort==='low'?a.price-b.price:filters.sort==='high'?b.price-a.price:b.createdAt.localeCompare(a.createdAt)); const set=(key:string,value:string)=>{const next=new URLSearchParams(params); value?next.set(key,value):next.delete(key); setParams(next)}; const reset=()=>setParams({}); return {filters,results,set,reset};}
