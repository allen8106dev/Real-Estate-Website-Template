import { SEO } from '@/components/layout/SEO'; import { PropertyCard } from '@/components/property/PropertyCard'; import { PropertyFilters } from '@/components/property/PropertyFilters'; import { usePropertyFilters } from '@/hooks/usePropertyFilters';
export default function Properties(){const x=usePropertyFilters();return <><SEO title="Properties"/><section className="bg-ink py-16 text-white"><div className="container-site"><p className="eyebrow">Browse with confidence</p><h1 className="mt-3 font-display text-4xl">Properties in Pune</h1><p className="mt-3 max-w-xl text-sm leading-6 text-white/75">A considered collection of homes, investments and commercial spaces.</p></div></section><section className="section container-site"><div className="grid gap-8 lg:grid-cols-[280px_1fr]"><PropertyFilters {...x}/><div><div className="mb-6 flex flex-wrap items-center justify-between gap-3"><p className="text-sm text-slate"><strong className="text-ink">{x.results.length}</strong> properties found</p><label className="flex items-center gap-2 text-sm">Sort <select className="field w-auto py-2" value={x.filters.sort} onChange={e=>x.set('sort',e.target.value)}><option value="newest">Newest</option><option value="low">Price: low to high</option><option value="high">Price: high to low</option></select></label></div>{x.results.length?<div className="grid gap-6 sm:grid-cols-2 xl:grid-cols-3">{x.results.map(p=><PropertyCard key={p.id} property={p}/>)}</div>:<div className="rounded-card bg-mist p-12 text-center"><h2 className="font-display text-2xl">No properties match that search.</h2><p className="mt-2 text-sm text-slate">Try broadening your filters to see more options.</p><button className="btn btn-primary mt-5" onClick={x.reset}>Clear filters</button></div>}</div></div></section></>}
