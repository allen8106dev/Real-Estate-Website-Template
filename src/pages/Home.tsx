import { Link } from 'react-router-dom'; import { ArrowRight,CheckCircle,ShieldCheck,MapPinned,Handshake } from 'lucide-react'; import { SEO } from '@/components/layout/SEO'; import { SearchBar } from '@/components/sections/SearchBar'; import { PropertyCard } from '@/components/property/PropertyCard'; import { AgentCard } from '@/components/agents/AgentCard'; import { CTA } from '@/components/sections/CTA'; import { properties } from '@/data/properties'; import { agents } from '@/data/agents'; import { locations } from '@/data/locations'; import { services,testimonials } from '@/data/content'; import { siteConfig } from '@/config/siteConfig';
const why=[[ShieldCheck,'Verified, considered listings','Every listing is assessed for clarity, quality and fit.'],[MapPinned,'Deep local knowledge','Advice grounded in the neighbourhoods we know intimately.'],[Handshake,'A transparent process','Straight answers and steady guidance from search to signature.']];
export default function Home(){return <><SEO/><section className="relative isolate overflow-hidden bg-pine"><img src="https://images.unsplash.com/photo-1600607687920-4e2a09cf159d?auto=format&fit=crop&w=2000&q=90" className="absolute inset-0 -z-10 h-full w-full object-cover opacity-45" alt="Contemporary residence"/><div className="container-site py-24 sm:py-32 lg:py-40"><p className="eyebrow">Pune real estate, thoughtfully handled</p><h1 className="mt-5 max-w-3xl font-display text-4xl leading-[1.1] text-white sm:text-6xl">Find a place that feels right from the first visit.</h1><p className="mt-6 max-w-xl text-base leading-7 text-white/80">{siteConfig.tagline} Discover verified homes, investments and commercial spaces with people who know Pune.</p><div className="mt-8 flex flex-wrap gap-3"><Link className="btn bg-gold text-ink hover:bg-white" to="/properties">View properties <ArrowRight size={16}/></Link><Link className="btn border border-white/40 text-white hover:bg-white hover:text-ink" to="/contact">Schedule a visit</Link></div></div></section><div className="container-site relative z-10 -mt-7"><SearchBar/></div><section className="section container-site"><div className="flex items-end justify-between gap-5"><div><p className="eyebrow">Handpicked for you</p><h2 className="mt-2 font-display text-3xl sm:text-4xl">Featured properties</h2></div><Link to="/properties" className="hidden text-sm font-bold text-gold sm:block">View all properties →</Link></div><div className="mt-9 grid gap-6 md:grid-cols-2 xl:grid-cols-4">{properties.filter(p=>p.featured).slice(0,4).map(p=><PropertyCard key={p.id} property={p}/>)}</div></section><section className="bg-mist"><div className="section container-site"><div className="max-w-xl"><p className="eyebrow">A better way to move</p><h2 className="mt-2 font-display text-3xl sm:text-4xl">Real estate is personal. Our approach stays that way.</h2></div><div className="mt-10 grid gap-7 md:grid-cols-3">{why.map(([Icon,title,copy])=>{const C=Icon as typeof ShieldCheck;return <div key={title as string}><C className="text-gold" size={28}/><h3 className="mt-4 font-display text-xl">{title as string}</h3><p className="mt-2 text-sm leading-6 text-slate">{copy as string}</p></div>})}</div></div></section><section className="section container-site"><p className="eyebrow">Explore Pune</p><h2 className="mt-2 font-display text-3xl sm:text-4xl">Neighbourhoods with a point of view</h2><div className="mt-9 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{locations.slice(0,4).map(l=><Link key={l.name} to={`/properties?location=${l.name}`} className="group relative aspect-[1.25] overflow-hidden rounded-card bg-ink"><img className="h-full w-full object-cover opacity-70 transition duration-500 group-hover:scale-105" src={`https://images.unsplash.com/${l.image}?auto=format&fit=crop&w=700&q=80`} alt={`${l.name}, Pune`} loading="lazy"/><div className="absolute inset-x-0 bottom-0 p-5 text-white"><h3 className="font-display text-2xl">{l.name}</h3><p className="mt-1 text-xs text-white/75">{l.count} curated listings</p></div></Link>)}</div></section><section className="bg-ink text-white"><div className="section container-site grid gap-8 text-center sm:grid-cols-4">{Object.entries(siteConfig.stats).map(([label,value])=><div key={label}><p className="font-display text-4xl text-gold">{value}+</p><p className="mt-2 text-xs uppercase tracking-wider text-white/70">{label.replace(/([A-Z])/g,' $1')}</p></div>)}</div></section><section className="section container-site"><div className="flex items-end justify-between"><div><p className="eyebrow">What we do</p><h2 className="mt-2 font-display text-3xl">Guidance at every stage</h2></div><Link className="text-sm font-bold text-gold" to="/services">All services →</Link></div><div className="mt-9 grid gap-px overflow-hidden rounded-card bg-ink/10 md:grid-cols-3">{services.slice(0,3).map(([title,copy])=><Link key={title} to="/services" className="bg-white p-7 transition hover:bg-mist"><CheckCircle className="text-gold" size={22}/><h3 className="mt-4 font-display text-xl">{title}</h3><p className="mt-2 text-sm leading-6 text-slate">{copy}</p></Link>)}</div></section><section className="bg-sand"><div className="section container-site"><p className="eyebrow">Client notes</p><h2 className="mt-2 font-display text-3xl">Trusted by people making meaningful moves</h2><div className="mt-9 grid gap-5 md:grid-cols-3">{testimonials.map(t=><figure key={t.id} className="rounded-card bg-white p-6"><div className="text-gold">★★★★★</div><blockquote className="mt-4 font-display text-lg leading-7">“{t.quote}”</blockquote><figcaption className="mt-5 text-sm font-bold">{t.name}<span className="block pt-1 font-normal text-slate">{t.role}</span></figcaption></figure>)}</div></div></section><section className="section container-site"><div className="flex items-end justify-between"><div><p className="eyebrow">Meet the team</p><h2 className="mt-2 font-display text-3xl">Local experts, steady partners</h2></div><Link className="text-sm font-bold text-gold" to="/agents">Meet everyone →</Link></div><div className="mt-9 grid gap-6 sm:grid-cols-2 lg:grid-cols-3">{agents.map(a=><AgentCard key={a.id} agent={a}/>)}</div></section><CTA/></>}
